[project]
name = "crypto-future-bot"
version = "1.0.0"
description = "Elite crypto futures trading algorithm with AI decision logic"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26.0",
    "pandas>=2.1.0",
]

[project.scripts]
backtest = "src.backtest:main"
optimize = "src.optimizer:main"
